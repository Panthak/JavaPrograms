import java.util.*;

public class findMaxOccurringNumber {
    private void findNumber(int[] arr) {
        int len = arr.length;
        Map<Integer, Integer> map = new HashMap<>();
        for(int i=0; i<len; i++) {
            if(map.containsKey(arr[i])) {
                map.put(arr[i],);
            }
        }
    }
    public static void main(String[] args) {

    }
}
